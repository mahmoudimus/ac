package com.atlassian.plugin.connect.modules.beans;

/**
 * This enum lists all types that a remote issue field can have. The currently supported types are string and text.
 * We plan to support singleSelect in the future.
 */
public enum IssueFieldType
{
    STRING,
    TEXT
}
