<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>Plugin which provides JIRA specific modules of Atlassian Connect.</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>

    <connect-request-filter key="upload-attachment-request-filter"
                            class="com.atlassian.plugin.connect.jira.web.UploadAttachmentRequestFilter" phase="after-api-scoping-filter"/>

</atlassian-plugin>